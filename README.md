
to install: clone and symlink\
the symlink name might change based on the coding agent used

```bash
git clone git@github.com:raulc0399/agents_setup.git
cd agents_setup
ln -s "$(pwd)/AGENTS.md" ~/.claude/CLAUDE.md
```